package com.isystem.demo;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class DemoApplication {

	public static void main(String[] args) {
		SpringApplication.run(DemoApplication.class, args);
	}

}
//makeBook.setAuthor(requestBook.getAuthor());
//makeBook.setId(requestBook.getId());
//makeBook.setMajor(requestBook.getMajor());
//makeBook.setLevel(requestBook.getLevel());
//makeBook.setName(requestBook.getName());